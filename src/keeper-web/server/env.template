# keeper-web server configuration

# Docket API
DOCKET_BASE_URL=https://localhost:57526
DOCKET_STUB_USER_ID=00000000-0000-0000-0000-000000000001

# Auth — stub for now, real validation comes in V3 RBAC
# The proxy passes this header to Docket on every request.
# Docket logs it but does not validate it yet.
KEEPER_API_KEY=keeper-dev-key

# Server
PORT=3001
DEBUG=true
